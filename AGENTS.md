# 项目开发约定

## 前端 UI 必读规范

任何涉及 `frontend/` 的页面、组件、样式、弹窗、下拉、表格或响应式改动，**开始编码前必须先阅读 [docs/UI开发规范.md](docs/UI开发规范.md)**。

优先复用全局 token、Naive UI Theme 和 `frontend/src/components/ui/` 中的组件；不要在业务页面创建新的默认样式体系或裸永久删除确认。
